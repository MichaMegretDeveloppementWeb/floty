<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <!--
        Pas de bloc <coverage> ici · il exigerait un driver (Xdebug / PCOV)
        installé localement pour chaque dev (non souhaité, ralentit la suite).
        Mesure de couverture ponctuelle possible en CLI si besoin ·
            php artisan test - -coverage-clover=coverage/php/clover.xml
        (lance temporairement Xdebug/PCOV pour le run).

        Pas de workflow GitHub Actions en V1 (doctrine solo dev avec pipeline
        pre-commit local · `composer test` + `pint` + `phpstan`). À mettre en
        place comme chantier dédié si introduction de CI pull-request.
    -->
    <php>
        <!--
            CRITIQUE · `force="true"` obligatoire sur les variables qui
            doivent override .env (sinon Laravel charge `.env` AVANT que
            phpunit applique l'override · si la variable est déjà set,
            l'override est ignoré · les tests tapent alors sur la DB dev).
            Précédent incident · 2026-05-17, RefreshDatabase a flushé la
            base `floty` (dev) au lieu de `floty_testing` (testing) parce
            que DB_DATABASE n'avait pas le `force`. Garde-fou ironclad ·
            tout `<env>` qui contredit `.env` DOIT avoir `force="true"`.
        -->
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <!-- Cache et sessions en mémoire pour la vitesse, mais la DB de
             tests est MySQL (mêmes features qu'en prod : CHECK, triggers,
             colonnes générées, JSON, FK RESTRICT…). -->
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="mysql" force="true"/>
        <env name="DB_DATABASE" value="floty_testing" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
