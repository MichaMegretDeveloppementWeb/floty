/**
 * Barrel des types API.
 *
 * Placeholder pour V2 quand on aura besoin de wrappers (pagination,
 * réponses d'erreur structurées, etc.). Aujourd'hui les payloads
 * Inertia/JSON sont déjà typés via `App.Data.*` (Spatie + TS Transformer).
 */
export {};
